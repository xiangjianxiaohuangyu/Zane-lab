{
  "compilerOptions": {
    // 编译目标：ES2020，支持现代浏览器特性
    "target": "ES2020",

    // 使用 defineProperty 定义 class 字段，符合现代 JS 标准
    "useDefineForClassFields": true,

    // 包含的库定义
    "lib": ["ES2020", "DOM", "DOM.Iterable"],

    // 模块系统：ESNext，支持现代模块语法
    "module": "ESNext",

    // 跳过类型声明文件的检查，提高编译速度
    "skipLibCheck": true,

    // 模块解析：bundler 模式，适用于 Vite 等现代构建工具
    "moduleResolution": "bundler",

    // 允许导入 .ts 扩展名
    "allowImportingTsExtensions": true,

    // 解析 JSON 文件
    "resolveJsonModule": true,

    // 独立模块处理，提高构建速度
    "isolatedModules": true,

    // 不生成输出文件（由 Vite 处理）
    "noEmit": true,

    // JSX 转换：使用 React 17+ 的新 JSX 运行时
    "jsx": "react-jsx",

    // 严格模式：启用所有严格类型检查选项
    "strict": true,

    // 报告未使用的局部变量
    "noUnusedLocals": true,

    // 报告未使用的参数
    "noUnusedParameters": true,

    // 检查 switch 语句中的 fallthrough
    "noFallthroughCasesInSwitch": true,

    // 基础路径：项目根目录
    "baseUrl": ".",

    // 路径别名：@ 映射到 src 目录
    "paths": {
      "@/*": ["./src/*"]
    }
  },

  // 包含的文件范围
  "include": ["src"],

  // 引用其他 TypeScript 配置文件
  "references": [{ "path": "./tsconfig.node.json" }]
}
