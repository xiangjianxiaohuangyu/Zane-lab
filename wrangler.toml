# Cloudflare Pages 部署配置
# 使用命令: npx wrangler pages deploy dist

name = "zane-lab"
compatibility_date = "2024-01-01"

# Pages 项目配置
[pages]
# 构建输出目录
project = "./dist"

# 环境变量（可选）
# [pages.vars]
# ENVIRONMENT = "production"
