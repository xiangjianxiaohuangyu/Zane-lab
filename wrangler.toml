# Cloudflare Pages 项目配置
# 部署命令: npx wrangler pages deploy dist --project-name=zane-lab

name = "zane-lab"
compatibility_date = "2024-01-01"

# Pages 项目配置
pages_build_output_dir = "./dist"

# 环境变量（可选）
# [vars]
# NODE_ENV = "production"
